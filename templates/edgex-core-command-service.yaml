apiVersion: v1
kind: Service
metadata:
  labels:
    org.edgexfoundry.service: {{.Values.edgex.command.name}}
  name: {{.Values.edgex.command.name}}
spec:
  ports:
  - name: "48082"
    port: 48082
    targetPort: 48082
  selector:
    org.edgexfoundry.service: {{.Values.edgex.command.name}}
  type: LoadBalancer
status:
  loadBalancer: {}
