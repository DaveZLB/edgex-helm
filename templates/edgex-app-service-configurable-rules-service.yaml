apiVersion: v1
kind: Service
metadata:
  labels:
    org.edgexfoundry.service: {{.Values.edgex.appServiceRules.name}}
  name: {{.Values.edgex.appServiceRules.name}}
spec:
  ports:
  - name: "48100"
    port: 48100
    targetPort: 48100
  selector:
    org.edgexfoundry.service: {{.Values.edgex.appServiceRules.name}}
status:
  loadBalancer: {}
